# Session Chronicle

Chronological log of session summaries.

- **[2026-04-21 15:45]** — プロジェクト立ち上げ・要件定義／アーキテクチャ策定。QRコード生成ツール凍結を受けた光学的テキスト持ち出し方式として`Nshinomiya/embodied-ai`の`wifi-cam-mcp`流用を決定。OCRはPAL経由`google/gemini-2.5-flash`主力（コスパ重視）。方式B（連続撮影＋ページ結合）をMVPとし、既存MCP無改造＋スラッシュコマンド層で実装する方針。保存先は`00_Inbox/`既定。開発リポジトリは別途用意。
- **[2026-04-21 16:10]** — PAL（gpt-5.1）によるレビューを反映。重要な設計変更: (1)ページ結合をLLMプロンプトからRapidFuzzベースの決定論的アルゴリズムに変更、(2)OCRをGemini Flash主力＋疑義ページのみHaiku二次OCRのハイブリッド化、(3)終了判定を`---END---`＋画像SSIM連続同一の二重化、(4)撮影前ラプラシアン分散ブレ検知追加、(5)成功基準を「誤字ゼロ」から「疑義ハイライト率＋誤字率」に変更、(6)VSCode Word Wrap OFFを撮影運用要件に明記、(7)HDMIキャプチャをバックアップ案として記載。結合アルゴリズム仕様（擬似コード＋疑義検知ルール）を別ドキュメント化。
- **[2026-04-21 19:12]** — handoverスキルでセッション全体のまとめを生成。旧HANDOVER.mdを`archive/handover-20260421-1912.md`に退避し、要件定義・アーキテクチャ・結合アルゴリズムの3ドキュメントを中核とする体制で次セッション（開発リポジトリ構築・ローカル結合モジュール実装）へ引き継ぎ。
- **[2026-04-22 00:30]** — gpt-5.2＋gemini-2.5-proのコンセンサスレビューを実施し、3ドキュメントに多数の指摘を反映（F-11〜F-16追加・結合アルゴリズムのin_codeバグ修正・MIN_SCORE引き上げ・リサイズMVP格上げ等）。ユーザー確認事項（撮影場所・HDMIポリシーNG・情報機密度・前処理方針）も反映。pytapo破断対策をRTSPフォールバックに確定。
- **[2026-04-28 17:12]** — embodied-ai 開発環境構築：uv / ffmpeg をインストールし `scripts/install-mcps.sh` で全 MCP 依存を一括導入。`.mcp.json` で wifi-cam / memory / system-temperature / sociality を接続、Tapo C220（`192.168.10.118`）越しの視覚応答まで確認。ハマりは IP 取り違え（`.1.118` → `.10.118`）と memory DB 用 `~/.claude/memories/` 未作成。memory-mcp 実装が ChromaDB ではなく SQLite + sentence-transformers + BM25 + Hopfield であることを発見し CLAUDE.md を修正。
- **[2026-04-28 17:48]** — screen-read 実装着手：`screen_read/` を uv パッケージとして作成し `merge.py` に決定論的ページ結合アルゴリズムを実装。14 ケースのユニットテスト（in_code マスクのスライス耐性・正規化漏れ防止・低多様性偶発マッチ抑制・MERGE_UNCERTAIN 挙動）が全て通過、ruff も clean。次は撮影前処理（ブレ検知・EXIF 除去・リサイズ）。
- **[2026-04-28 18:43]** — screen-read 撮影前処理を実装：`preprocess.py` にラプラシアン分散ブレ検知・EXIF 除去（exiftool 優先 + Pillow フォールバック）・長辺 ≤2000px リサイズ・撮影クールダウン定数を追加。重い依存（cv2/PIL/numpy）は遅延インポートで preprocess extra 切り離しを維持。pytest 6 ケース追加で計 20 通過、ruff clean。スラッシュコマンドは `.claude/commands/` ではなく `.claude/skills/screen-read/` で実装する方針に転換（自動発動が自然なため）。
- **[2026-04-28 20:18]** — screen-read オーケストレーション層を実装：`.claude/skills/screen-read/SKILL.md` に撮影→前処理→OCR→疑義→結合→Obsidian 保存の全フロー（F-1〜F-16 網羅）と、`scripts/screen_read_helper.py` に JSON 出力 CLI 4 サブコマンド（preprocess / same-page / save-page / merge-save）を追加。ドライランで k=3 score 100.0 結合・EXIF 除去・frontmatter 生成を確認。`save-page` の `.rstrip()` で OCR 末尾改行による境界整列ズレを防止。
- **[2026-04-28 22:35]** — 実機 1 ページテスト（VSCode 日本語ダークモード）を完走：Tapo C220 でプリセット `screen-read` 経由の撮影 → preprocess（variance 2407）→ Gemini 2.5 Flash OCR → frontmatter 付き Markdown 保存。SKILL.md にプリセット運用・実ファイル取得手順・OCR システム指示強化（行番号/オーバーレイ/外側 ```markdown フェンス無視）を反映。auto-aim は MVP には入れず、プリセット上書き運用が最小コストと判断。
- **[2026-04-29 09:59]** — 実機 2 ページ統合テスト中に PAL `chat` ツールが OCR 用途で構造的に不適と判明（Anthropic / OpenAI 系 vision モデルは persona injection で拒否、Gemini Pro でも boilerplate 捏造）。意思決定: OCR を **OpenRouter API 直叩き**に標準化。helper に `ocr` サブコマンド（urllib.request, F-15/F-16 内蔵, _strip_outer_fence）を追加し、SKILL.md を `mcp__pal__chat` → `$HELPER ocr` へ切替。F-9 二次 OCR は Haiku（image 非対応）から `gemini-2.5-pro` に変更。夜帯撮影で Gemini Flash がコントラスト不足画像をハルシネーションした事象も観察、preprocess へのコントラスト検知追加が次回優先課題。
- **[2026-04-29 11:30]** — `helper ocr` の live 動作確認と F-9 二次 OCR 効果の実数測定。Flash $0.002/page・Pro $0.051/page で Pro が `OPENROUTER_API_KEY` / `.env` / `Crunched for 6m 37s` 等の Flash 誤読をほぼ修正することを確認。1 セッション $0.10 程度で要件 < 10 円を満たす。本セッションは検証で打ち止め、次セッションで preprocess の輝度ゲート追加と 2 ページ統合テストを合わせて実施する方針。`OPENROUTER_API_KEY` は環境変数（`~/.bashrc`）採用に決定。
- **[2026-04-29 14:47]** — preprocess 輝度ゲートと F-13 救済 UI を実装。`measure_brightness()`（mean < 60 OR std < 20 で low_contrast）でブレ検知単独では捕捉できない暗所ハルシネーションを撮影前に弾く。F-13 は `merge-save` の戻り値を `boundaries` 配列に拡張し、各境界の画像パス + OCR スニペットを乗せる方針に変更。`inspect-boundary` / `apply-boundary-fix` の 2 サブコマンドで境界手修正フローを完結。pytest 23 通過、ruff clean、dry-run で 3 段連携を確認。次セッションは実機 2 ページ統合テストから再開。
- **[2026-04-29 18:50]** — 実機 4 ページ統合テストを完走。輝度ゲートは 4 枚すべて通過、Pro 二次 OCR で固有名詞・記号が劇的改善、F-13 救済 UI（C 案: 空 replacement でマーカー除去）が実用ベースで動作。検出された宿題 3 件: (1) `_list_page_files` glob が `page-*.ocr.json` を拾ってクラッシュ、(2) page-N が page-(N-1) を完全包含する撮影パターンで overlap 検出が破綻、(3) Flash の構造的誤読（行端・インデント変則）が境界検出を壊す可能性。最終 Markdown は `~/obsidian-vault/00_Inbox/clip-20260429-1743.md` に出力（重複領域は手動編集対象）。コミットなし、次セッションで一括修正する方針。
- **[2026-04-29 22:48]** — 前セッションで残った宿題 4 件を一気に消化: glob 衝突を 3 桁数字限定パターンに修正、Gemini 2.5 Pro を一次 OCR に格上げ（Flash はクロスチェック降格）、`docs/test-images/` を画像アーカイブとして `.gitignore` 追加、Vault 既定パスを Windows 側 `/mnt/c/Users/SlmbrCat/Documents/obsidian/pkm` へ切替。包含 boundary の救済機能は YAGNI と判断し、SKILL.md の戻し量ガイド + F-13 + 手動編集の 3 段で対処する方針。残タスクは輝度ゲート閾値の実機チューニング（運用後検討）のみ。
- **[2026-05-04 12:30]** — screen-read を後回しにして memory-mcp / sociality-mcp の機能調査・初期設定に集中。Plan エージェントで両サーバーの実装を精読しリファレンスドキュメント（`docs/memory-sociality-overview.md`）を作成。memory.db にユーザー好み 4 件を core カテゴリで保存。フック・デーモン類の現状診断を実施し、social.db 未作成・heartbeat daemon 未起動の 2 点が残課題と判明。
- **[2026-05-04 16:17]** — sociality スタック全体の person_id を "kouta" → "natsuko" に統一（5 ファイル）。`upsert_person` で social.db を初期生成、cron で heartbeat daemon を WSL2 起動、compose → plan フローの動作確認（relevant_memories に memory.db の記憶が乗ることを確認）。インフラ残課題は自律ループと ingest_interaction の運用設計のみ。
- **[2026-05-05 13:32]** — コード変更ゼロの設計セッション。`record_agent_experience` の運用設計を起点に、プロジェクト全体を「身体メタファー三層（下位＝知覚＝自動 / 上位＝行動＝Claude 判断 / メタ＝自己認識補助＝自動）」で再整理。auto-social.sh を廃止して Claude 自身の `ingest_interaction` 呼び出しに切り替える方針確定。CLAUDE.md（424 行）を縮約し、MCP ツール表を `.claude/rules/`（パス条件付き）へ、Heartbeat Protocol を `.claude/skills/interaction/`（タスク単位）へ移管する段階移行計画を策定。
- **[2026-05-06 15:31]** — 設計移行 Step 1〜4 を一気に消化。`.claude/skills/interaction/SKILL.md` を voice 拡張込みで新設（216 行、compose→plan→act→record の対話ループ）、`auto-social.sh` を廃止して `ingest_interaction` 経由に切替、CLAUDE.md を 424→45 行に縮約して `.claude/rules/` 9 ファイルへ機能別に分割（paths はカンマ区切り）。Plan エージェントを起動して「テキスト + 音声インタラクティブ会話」マイルストーン到達計画（Phase 1〜4）と Step 5 メタ層の観測ベース着手方針を策定。次は Phase 1（`.mcp.json` への tts-mcp 追加）から。
- **[2026-05-06 21:35]** — Phase 1（コネクタ層）と Phase 2（データ層）を消化。`.mcp.json` に `tts` エントリを ElevenLabs + VOICEVOX 両対応で集約（`.env` ではなく `.mcp.json` の env 直書きに統一、wifi-cam と同じスタイル）。`append_daybook` を fallback 日付（2026-01-01）と今日付（2026-05-06）の 2 回呼んで `latest_daybook` の non-null 化を検証、副次的に interaction skill 試運転 E-1 の主要項目（compose / ingest×2 / record_agent_experience）も実機で動作確認。`record_agent_experience` の payload は `experience_kind` ではなく `kind` であることを発見し HANDOVER に記録。残作業は API キー入力 + Claude Code 再起動 → Phase 3 微調整 → Phase 4 フル試運転。
- **[2026-05-07 19:23]** — Phase 3（interaction skill ガイド微調整）を消化。SKILL.md 冒頭に voice 入力時の `voice.speak` 上書きチェックリストを目立つ位置で追加（位置バイアス対策で重複配置）、4-b の record_agent_experience 表を `payload.kind` 起点に再構成して `experience_kind → kind` 変換ルールを明示、tts.md にエンジン使い分けガイドとアンチパターン節を追加、socialPolicy.toml の natsuko avoid_actions に `loud_voice_during_quiet_hours` 追加。**コミットはユーザー判断で拒否**されたため次セッション開始時は dirty working tree。副次的に `/statusline` を 2 段階で構成（PS1 起点 → モデル/コンテキスト/セッションリミット表示）。次は Phase 1 完遂（API キー入力 + Claude Code 再起動）→ Phase 4 フル試運転。
